import React from 'react';

const ErrorExample = () => {
  //
  const increment = () => {
    count + 1;
  };

  return (
    <>
      <h2>{count}</h2>
      <button onClick={increment}>Increment</button>
    </>
  );
};

export default ErrorExample;
